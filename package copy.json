{
  "scripts": {
    "dev": "vite",
    "preview": "vite preview",
    "build": "vite build && cp -r dist docs",
    "rebuild": "vite build && xcopy /e /i /y seo dist && rmdir /s /q docs && xcopy /e /i /y dist docs",
    "git": "git add . && git commit && git push origin main"
  },
  "devDependencies": {
    "@types/node": "^22.13.14"
  },
  "dependencies": {
    "@tailwindcss/postcss": "^4.0.17",
    "postcss": "^8.5.3",
    "tailwindcss": "^4.0.17"
  }
}
